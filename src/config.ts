/** URL of a Git repository to redirect to from the root / route. */
export const GIT_REPO_URL = "https://github.com/dreamscached/httpjax";

/** Max size (in bytes) of TeX body content (in /render routes.) */
export const MAX_TEX_BODY_SIZE = 1024;
